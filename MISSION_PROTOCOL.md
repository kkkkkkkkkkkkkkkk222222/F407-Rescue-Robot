# 当前普通物资抓取与投送流程

本固件按`danmo-teng/shijue_fangan`的`84e8ab0`任务命令语义工作，运行模式为`APP_ENABLE_TASK=1`，其他测试和独立视觉居中Task均关闭。RDK X5与F407使用USART3（PD8 TX、PD9 RX）、115200 8N1、3.3 V TTL和公共15字节帧。

## 整体流程

1. 上电后停车，摄像头回90°，舵机1到55°，左右爪依次收缩到左23°、右145°。
2. 收到合法`TYPE=0x11`红蓝方和出发区配置后回复一次`A3 B3 01 C3`，开始180秒任务。
3. 用编码器累计路程倒车1.70 m：车体始终按180°后退方向运动，同时由IMU锁定启动航向；剩余300 mm和100 mm时分别降到350 mm/s和160 mm/s，进入10 mm容差后制动。随后舵机1到85°，夹爪到Touch姿态，原地转180°并等待5秒。
4. 出发搜索之前先打开夹爪（左爪128°、右爪52°），以250 mm/s向物体堆前进0.20 m。接着以500 mm/s的旋转切向速度正向转满360°，停车250 ms，再反向转满360°，利用两次反向扰动撞散堆叠物体；最后以250 mm/s后退0.30 m，给摄像头留出观察距离。
5. 搜索时摄像头保持120°。进入每一轮`SEARCH`时，F407记录当时最后一帧合法视觉报告的本地接收代次；必须收到代次更新的新报告后才允许X/Y触发目标锁定，撞散阶段缓存的坐标即使仍在250 ms有效期内也只用于LCD显示，不参与控制。小车以160 mm/s原地旋转一圈；没有普通物资就前进0.8 m，再从新位置继续搜索。
6. 收到原生1280×1024目标坐标后，锁定进入接近阶段，不再返回搜索阶段。X控制底盘旋转，Y控制摄像头舵机3，默认以300 mm/s接近；如果上位机提供有效距离，则在500 mm和250 mm处分别减速到225 mm/s和125 mm/s。目标暂时丢失且舵机3低于140°时，只停车等待并保持当前角度；在140°～150°之间丢失目标时，舵机3直接转到150°并进入抓取观察。若150°仍无目标，则进入后续抬高摄像头和慢速旋转寻找流程。
7. 摄像头到150°后停车并持续通过`TYPE=0x17`置`CLAW_VISIBLE=1`，让上位机检查底部抓取窗。
8. 如果上位机报告画面无物体，摄像头每次抬高10°（舵机角度150→140→…→90），每次抬高后停车观察1秒；仍没有物体就以100 mm/s慢速旋转观察。转满一圈仍没有物体则再次抬高并重复。在视觉报告超时期间保持停车。
9. 上位机连续3帧确认物体进入`X=384..896、Y>=760`的底部抓取窗后发送`TYPE=0x18 / GRAB_CONFIRMED`。F407收到后合爪，完成后持续置`GRIPPER_CLOSED=1`。
10. 上位机发送安全区前置点。F407结合`TYPE=0x16`融合位姿计算目标方位，先原地转向，再沿车头方向直行；距离目标500 mm内从650 mm/s降到250 mm/s。
11. 上位机发送`ALIGN_SAFE_ZONE`后，F407转到命令给出的最终航向。收到`ENTER_SAFE_ZONE`后先张开夹爪，再以250 mm/s后退0.30 m，随后以750 mm/s加速前冲0.55 m撞送物体，停车观察600 ms。
12. 如果上位机仍发送`ENTER_SAFE_ZONE`，说明尚未确认物体进入安全区，F407重复“后退0.30 m→前冲0.55 m→观察”。上位机连续3帧确认普通物资位于本方安全区框内后发送`TASK_COMPLETE`，F407立即中止撞送，后退0.45 m，然后根据`TYPE=0x16`当前位置计算指向场地中心`(0,0)`的航向并原地对正，最后进入`SEARCH`。

当前上位机在发出`TASK_COMPLETE`后会停留在`COMPLETE`且不再输出下一轮视觉报告。F407虽然会回到`SEARCH`，但要继续识别下一件物体，上位机还需要在收到STM32的`TYPE=0x17`搜索状态后复位其任务状态机。

任何运动阶段发生电机故障、任务命令超过250 ms、融合位姿超过150 ms、T265质量无效或收到`STOP/ABORT`时立即停车并进入故障状态。

## 公共帧

```text
A3 B3 TYPE SEQ P0 P1 P2 P3 P4 P5 P6 P7 CRC_LO CRC_HI C3
```

CRC为CRC-16/Modbus，覆盖`TYPE、SEQ、P0..P7`共10字节，初值`0xFFFF`、多项式`0xA001`，低字节先发送。各消息类型独立使用SEQ，重复SEQ不刷新对应看门狗。

## 消息类型

| TYPE | 方向 | 作用 |
| --- | --- | --- |
| `0x11` | RDK→F407 | 颜色与出发区配置 |
| `0x12` | RDK→F407 | 原生1280×1024普通物资坐标报告 |
| `0x15` | F407→RDK | 100 Hz三路编码器累计低16位 |
| `0x16` | RDK→F407 | 20 Hz融合场地位姿 |
| `0x17` | F407→RDK | 20 Hz任务状态 |
| `0x18` | RDK→F407 | 抓取、导航、对正、入区和完成命令 |

### `TYPE=0x12`视觉报告

`P0/P1=X`、`P2/P3=Y`、`P4/P5=距离mm`，均为大端；`P6`为四类目标2位数量字段；`P7 bit0=FOUND、bit1=NEAR、bit3=CLASS_VALID、bit6=DISTANCE_VALID`。当前上位机直接发送0～1279和0～1023的原始坐标，中心为`(640,512)`。没有目标时P0～P7全0；没有可靠距离时距离为0且bit6清零。

已知中心帧：

```text
A3 B3 12 10 02 80 02 00 00 00 01 09 DD FD C3
```

### `TYPE=0x17`STM32状态

| 字段 | 内容 |
| --- | --- |
| `P0` | bit0爪子入镜、bit1夹爪闭合、bit2电机运动、bit3自动靠近、bit7故障 |
| `P1` | 当前`TaskState`编号 |
| `P2/P3` | 摄像头角度，0.01°，大端 |
| `P4` | 最近真正接受的`TYPE=0x18`命令SEQ |
| `P5` | 故障码，0为正常 |
| `P6/P7` | 固定为0 |

### `TYPE=0x18`任务命令

`P0=COMMAND、P1=FLAGS、P2/P3=TARGET_X_mm、P4/P5=TARGET_Y_mm、P6/P7=HEADING_cdeg`。X/Y为有符号int16大端；航向范围0～35999，单位0.01°。FLAGS bit0必须为1，bit1要求直行，bit2使用最终航向，bit3表示红方。

命令值：`0 STOP、2 GRAB_CONFIRMED、3 NAVIGATE_WAYPOINT、4 ALIGN_SAFE_ZONE、5 ENTER_SAFE_ZONE、6 TASK_COMPLETE、7 ABORT`。

红方前置点为`(0,+950 mm)`、最终航向90°；蓝方前置点为`(0,-950 mm)`、最终航向270°。F407不接收PWM值，只接收任务目标并在本地完成转向、速度限制和失联停车。
