# PCB 与 CubeMX 复核结论

依据 `F407原理图.pdf`、`扩展板.pdf`、`最终PCB方案.md` 与 `WWW.ioc` 交叉核对。当前收到的是原理图导出 PDF，没有 PCB 布局、Gerber 或设计规则报告，因此本结论能检查网络与配置，不能确认线宽、过孔载流、间距、回流路径和器件封装方向。

## 打板前建议修正

1. 扩展板 USB-C 的 CC1、CC2 悬空。作为 5 V 受电端，每个 CC 脚应各接一个 5.1 kΩ Rd 到 GND，否则 C-to-C 线缆下通常不会得到 VBUS。
2. 多个 5 V 电源存在反灌路径：扩展板 12 V 降压输出、扩展板 USB-C 5V_IN，以及开发板 USB VBUS/5V（开发板上二者经 FU1 相连）最终可能被并接。现版只能严格保证一次使用一个 5 V 来源；量产设计应增加电源选择跳帽、理想二极管或负载开关。
3. 开发板 SWD 口只有 3V3/GND/SWDIO/SWCLK/BOOT0，没有 NRST。J-Link/Ozone 普通 SWD 可工作，但程序误关 SWD、进入低功耗或时钟异常时不便 connect-under-reset。建议把 NRST 引到调试口。
4. 12 V 的 XT60 与 DC 插座直接并联，且 12 V 入口未看到保险、反接保护和浪涌钳位。应规定两个入口不可同时使用，或增加电源 OR-ing；机器人电机环境建议增加保险、反接保护和合适的 TVS。
5. 舵机 5V_BUS 与电机驱动电源附近缺少明显的大容量储能电容。建议在四路舵机接口附近放 470-1000 uF 低 ESR 电解加 10 uF/100 nF，在每块电机驱动模块附近补充本地去耦，并按堵转电流复核铜宽、连接器和 6 A 自恢复保险丝。
6. 四组编码器由 5V_OUT 供电，信号直接进入 MCU。所选 GPIO 属数字可用引脚，但仍需确认编码器输出高电平和上电时序满足 STM32 输入限制；更稳妥的做法是编码器改用 3.3 V、加串联限流，或使用 3.3 V 施密特缓冲/电平转换，避免 MCU 断电时由信号脚反向供电。

## 已确认正确的网络

- 扩展板与开发板排针的 GPIO 编号能够对应。
- 8 MHz HSE 与开发板晶振一致，168 MHz 主时钟方案可用。
- 三路硬件编码器网络为 PE9/PE11、PA6/PA7、PD12/PD13。
- 第四路编码器实际为 PD3/PD4；PC6-PC9 实际接四路舵机。
- 四组 AT8236 输入分别为 PA2/PA3、PE5/PE6、PB10/PB11、PB8/PB9。
- 新PCB的“串口1”是USART3（PD8 TX、PD9 RX），当前用于RDK X5通信，115200 8N1；“串口2”是USART1（PA9 TX、PA10 RX），当前保留。连接时需要TX对RX、RX对TX，并共地。
- TFT 连接为 PB13 SCK、PB15 MOSI、PB12 CS、PB14 RESET、PC5 DC/RS、PB1 BL。开发板原理图只标出了排针网络名，功能顺序按成品板配套 TFT 接口与原始 GPIO 标签确认。

## 原方案文档中的冲突

- 文档前半段和 PCB 都是 M4 编码器 PD3/PD4、舵机 PC6-PC9；后半段任务说明却写成 M4 使用 TIM8 PC6/PC7、舵机使用 PE12-PE15 软件 PWM。后半段不适用于这版 PCB。
- 文档标题写“舵机软件 PWM”，但 PC6-PC9 已连接 TIM8 CH1-CH4，直接使用硬件 PWM 更准确、更稳定。
- 方案文档曾把 PB14/PC5 写反。正确配置为 PB14 RESET、PC5 DC/RS；若反接，PB14 会被当作 DC 长期拉低，实际屏幕一直处于复位状态，只能看到白色背光。
- LCD 的 PB12 在原 `.ioc` 中配置成硬件 NSS。ST7735 需要按命令事务控制 CS，已改为普通 GPIO 软件片选。

## 已修正的 CubeMX 参数

- PLLQ：4 -> 7，得到标准 48 MHz PLL48CLK。
- SPI2：软件 NSS、10.5 Mbit/s、Mode 0；ST7735 在上升沿采样，Mode 0 是常见且便于兼容的配置。
- TIM8：PSC 167、ARR 19999，得到 50 Hz 和 1 us 计数分辨率。
- TIM9：PSC 1、ARR 4199，得到 20 kHz。
- TIM10/TIM11：PSC 0、ARR 8399，得到 20 kHz；原配置实际为 50 Hz，不适合电机。
- TIM6、DMA1 Stream1、USART3：中断向量和NVIC已配置；USART3 RX使用循环DMA。USART1原配置保留但不参与当前RDK通信。
