# 临时底盘修改交接记录

## 1. 修改范围与基线

- 日期：2026-09-04
- 底盘基线：`d431e37 feat: optimize autonomous motion and recovery`
- 对照上位机：`danmo-teng/shijue_fangan@4bbbb83`（抓取超时预计由队友后续提高到3秒）
- 正式RDK通信保持为PCB串口1 / USART3（PD8 TX、PD9 RX）、115200 8N1、固定15字节帧。
- 本次目标是删除开局盲打乱、降低抓取俯仰阈值、提高抓取命令和返航链路容错，并增加不干扰正式协议的舵机调试入口。
- 修改前尚未完成的结构重构已单独保存在本地分支`wip/refactor-before-temp-chassis-20260904`，提交`c0dc7c8`，没有混入本次改动。

## 2. 启动与搜索

开局定距和航向参数没有修改：

1. 上电依次把左爪收至23°、右爪收至147°。
2. 收到合法赛前配置后，编码器累计路程倒车1.70 m。
3. 倒车方向为车体180°，`Motor_MoveAngle()`持续保持动作开始时的IMU航向。
4. 前600 mm障碍区速度为800 mm/s，之后中心巡航为900 mm/s；剩余100 mm仍降到160 mm/s，10 mm容差内停车。
5. 舵机1到85°，夹爪进入Touch姿态，IMU闭环原地转180°，保留原有5秒等待。
6. 打开夹爪（左108°、右72°）后直接进入`SEARCH`。

旧的开局盲打乱状态、函数和参数已经删除，流程固定为`START -> OPEN_CLAW -> SEARCH`。原状态值19～23保留为空洞，后续状态编号不变，避免影响现有诊断。上位机通过视觉确认首件绿色物资无法单独取得后发送的`DISPERSE_PILE`属于受控任务算法，F407在空爪且没有等待夹内复审时继续接受该命令。

进入每一轮`SEARCH`时仍记录视觉报告本地代次，只有进入搜索后收到的新报告才能触发`APPROACH`，因此开局定距、转向和开爪期间缓存的X/Y不会干扰搜索。

## 3. 抓取角度与命令容错

准备抓取的舵机3阈值由150°改为140°：

```c
#define APP_GRAB_VIEW_ANGLE           140U
#define APP_GRAB_LOSS_FORCE_ANGLE_MIN 140U
```

本机构中舵机3角度越大，相机看得越低。因此数值150→140表示机械上更早停止下压并进入抓取观察。

队友在`4bbbb83`中新增`GRABBING`状态。连续视觉确认目标后，上位机以20～50 Hz反复发送`GRAB_CONFIRMED`，只有新鲜STM32状态帧报告`GRIPPER_CLOSED=1`才转入`NAVIGATE`。

底盘兼容规则：

- 抓取观察/抬高/旋转观察状态第一次收到`GRAB_CONFIRMED`时进入`TASK_CLOSE_CLAW`。
- `TASK_CLOSE_CLAW`和`TASK_WAIT_NAVIGATION`中每个新GRAB SEQ都更新`TYPE=0x17 P4 acknowledged_sequence`，但不重新启动舵机。
- `Claw_Touch()`两步动作真正完成后才置`GRIPPER_CLOSED=1`，此后20 Hz持续上报，不发一次性脉冲。
- 旧版“先收到NAV则补做合爪”兼容逻辑已删除；新协议在确认闭合前不得发NAV。
- 合爪完成后底盘留在`TASK_WAIT_NAVIGATION`，收到后续NAV才进入正常返航。

时序注意：左右爪现已同时开始合拢，但底盘仍等待2秒后才置`GRIPPER_CLOSED`，因此上位机抓取超时必须大于2秒并预留UART/状态转发余量；已与队友约定使用3秒。

为避免低帧率或舵机3尚未到140°时底盘继续顶着物体前进，`APPROACH`增加了近距停车保护：合法锁定目标横向误差不超过96 px，并且上位机置`NEAR`或有效距离不大于120 mm时，底盘立即停车并锁存该状态。停车后舵机3按原扫描步进继续移到140°，随后才置`CLAW_VISIBLE`进入抓取观察；真正闭爪仍只响应`GRAB_CONFIRMED`。若目标中途丢失，原1.2秒目标丢失恢复仍然生效。舵机3达到135°后，尚未触发近距停车的前进速度额外限制为80 mm/s，以减小最终制动距离。

空旷移动阶段采用分段提速：搜索完整两圈仍无目标时的0.8 m换位速度为850 mm/s；物体投送完成后的`RETURN_CENTER`巡航速度为800 mm/s，剩余300 mm内继续沿用原有最高400 mm/s及线性降速。抓取靠近、搜索旋转、放置区最终对正和慢推速度均未修改。

## 4. 返航断流保护

当前上位机的`NAVIGATE`状态不依赖画面内仍能看到物块，而是持续发送固定安全区前置点。舵机3回90°本身不会让状态机停止NAV。实际薄弱点是上位机只在出现新相机帧时更新命令文件，而原底盘对250 ms任务命令超时和150 ms融合位姿异常都直接进入永久故障。

本次只调整`TASK_NAVIGATE`：

| 条件 | 底盘行为 |
| --- | --- |
| NAV命令年龄≤250 ms，融合位姿有效 | 正常导航，远距离最高800 mm/s |
| NAV命令年龄250～1000 ms，融合位姿有效 | 沿已确认目标点继续，限速250 mm/s |
| NAV命令年龄>1000 ms | 立即停车并保留NAV状态，新命令恢复后重新对正并继续 |
| 融合位姿超过150 ms、T265非GOOD或更新被拒绝 | 立即停车并保留NAV状态，位姿恢复后重新对正并继续 |
| NAV中收到STOP | 立即停车但保留NAV状态，新NAV到达后恢复 |
| ABORT、其他阶段STOP、电机故障、1.5秒无导航进展 | 故障停车 |

命令刚断流后的理论附加运动上限约为：

```text
800 mm/s × 0.25 s + 250 mm/s × 0.75 s ≈ 388 mm
```

融合位姿无效时不会执行这段保持，不存在使用旧位姿盲跑。`ALIGN_SAFE_ZONE`、`ENTER_SAFE_ZONE`及撞送动作继续使用原有严格看门狗。

## 5. LCD任务界面

正常靠近界面保持为：

```text
STATE:APPROACH T:...
X:0640 Y:0512
CMD:GRAB OK
Angle:140
```

抓取和返航阶段改为显示更有用的握手数据：

```text
STATE:CLOSE T:...
GRIP:WAIT A:123
CMD:GRAB OK
Angle:140

STATE:NAV T:...
H:090 AGE:0045
CMD:NAV OK
Angle:090
```

`A`是最近确认的GRAB SEQ；`H`是上位机实时返航航向，`AGE`是该命令的本地年龄（ms）。`CMD`始终显示最近收到的原始动作，新协议持续重发GRAB，因此删除了1秒显示锁定变量。

- `OK`：该任务命令不超过250 ms。
- `HOLD`：NAV命令处于250～1000 ms降速保持期。
- `TMO`：任务命令已经超时。
- 尚未收到任务命令时显示`NONE`，后缀显示USART3整体接收状态。

## 6. 舵机调试模式

调试入口使用PCB串口2 / USART1（PA9 TX、PA10 RX），配置为115200 8N1，便于山外等通用串口助手直接选择。它不占用RDK的USART3，也没有修改正式15字节协议。

连接3.3 V USB-TTL并发送以回车或换行结尾的ASCII命令：

```text
DEBUG       进入调试模式，立即停车并暂停Task
S 1 55      舵机1转到55°
S 2 100     舵机2转到100°
S 3 140     摄像头舵机3转到140°
S 4 80      舵机4转到80°
RUN         停车并复位MCU，重新进入正常模式
```

规则：

- `S`命令只在`DEBUG`之后执行。
- 舵机编号只允许1～4，角度只允许0～180；舵机3继续受`Camera_SetAngle()`的0～170°软件限位。
- 合法命令返回`OK`，非法命令返回`ERR`。
- 调试模式暂停高层Task，但USART3视觉解析、编码器上传、IMU和LCD仍运行，可同时观察X/Y、上位机动作和相机角度。
- LCD首行显示`MODE:DEBUG`，末行显示最近手动设置的舵机和角度。
- `RUN`采用MCU复位而不是恢复被暂停的中间状态，避免继续执行过期的电机或机构步骤。复位后必须让上位机重新发送`TYPE=0x11`配置；最简单的方法是重新启动`mission_test`。
- PA0按键仍不参与任务启动或模式切换。

如不需要该调试入口，可将`APP_ENABLE_RUNTIME_SERVO_DEBUG`改为0；USART1接收中断不会启用。

## 7. 修改文件

- `Main/Inc/app_config.h`：临时打散开关、140°抓取阈值、NAV 1000 ms保持窗、运行时调试开关。
- `Main/Src/Task.c`：绕过打散、重复GRAB幂等ACK、实时返航航向、NAV断流/STOP/位姿恢复策略。
- `Main/Inc/DebugConsole.h`、`Main/Src/DebugConsole.c`：USART1文本调试模式。
- `Main/Src/Robot.c`：初始化和轮询调试控制台，调试时暂停Task。
- `Main/Inc/Lcd.h`、`Main/Src/Lcd.c`：显示上位机动作与调试状态。
- `CMakeLists.txt`：加入`DebugConsole.c`。
- `MISSION_PROTOCOL.md`、`README.md`：同步流程和操作说明。

## 8. 验证与现场测试顺序

已完成的静态验证应记录在提交说明中；烧录前后按以下顺序做台架测试：

1. 架空车轮，确认前600 mm以800 mm/s执行编码器定距，舵机1保持55°、双爪保持Retract且只做航向保持；定距完成后舵机1到85°、双爪同时打开并以最高900 mm/s开始闭环转向。累计倒车1.70 m，只在最后100 mm减速，到达后不再单独转向或等待5秒。
2. 确认LCD从`START`经极短的`OPEN`直接进入`SEARCH`，不出现`PILEIN/SPIN+/SPIN-/PILEOUT`。
3. USART1发送`DEBUG`，确认车轮停止；分别发送4路舵机角度，核对机械限位；发送`RUN`后重新启动上位机。
4. 以20～50 Hz注入不同SEQ的`GRAB_CONFIRMED`，确认只启动一次合爪、LCD ACK持续跟随，动作完成后`GRIPPER_CLOSED`持续为1。
5. 合爪未完成时注入NAV，确认不再补抓或进入返航。
6. 返航注入带`DRIVE_STRAIGHT | USE_FINAL_HEADING | DISTANCE_VALID`的航向＋距离，确认先对向再定距；剩余300 mm时为巡航上限，NAV接近终点降到160 mm/s、RETURN降到120 mm/s。暂停任务命令约0.5秒，确认LCD显示`HOLD`且速度上限降到250 mm/s；定距开始后暂停超过1秒应故障停车，不能恢复后重跑完整距离。
7. NAV尚未启动定距时注入`STOP`，确认停车等待并可由新NAV恢复；定距已经启动后注入`STOP`，必须以`COMMAND_TIMEOUT`故障停车，防止恢复时重跑完整距离。注入`ABORT`始终永久故障停车。
8. 默认不再发送`TYPE=0x16`。完成首件普通物资后注入`TASK_COMPLETE`，确认F407退出安全区并等待`RETURN_CENTER`；返中定距完成进入SEARCH后，上位机应清除旧目标并允许四类单目标开始下一轮。
9. 确认安全区对正和撞送的250 ms严格失联保护未被放宽。

## 9. 已知限制

- 开局到中心使用编码器路程和IMU航向保持，不是T265绝对坐标闭环；轮胎持续同向打滑会造成实际距离偏差。
- 假设中心附近只有一个物块；SEARCH会依次完成120°和90°视角的两次整圈扫描，两圈均漏检后才向新位置移动0.8 m。
- NAV断流保持只能覆盖短暂调度或视觉卡顿，不能替代上位机命令ACK/重发。
- 连续搬运已依赖`RETURN_CENTER`握手；若上位机未运行到`c576447`或未转发命令8，F407会停在`CENTER`等待，不会自行猜测返中距离。

## 10. 2026-09-05旧版协议复核项

对照`danmo-teng/shijue_fangan@c09aef2`时确认以下小差异，当前先记录、未修改：

1. 未闭爪时提前收到`NAVIGATE_WAYPOINT / ALIGN_SAFE_ZONE / ENTER_SAFE_ZONE`仍只会被忽略；最新RDK状态机保证确认`GRIPPER_CLOSED`后才发NAV。若现场发现提前命令，需要双方共同增加显式`WAIT_GRIPPER`状态，不能用NAV触发补抓。
2. 已解决：`ALIGN_SAFE_ZONE`和定距不再依赖周期性融合位姿，使用本地Location/IMU；本地定位无效时停车等待。
3. 已解决：NAV和RETURN现在同时强制要求`DRIVE_STRAIGHT | USE_FINAL_HEADING | DISTANCE_VALID`，不再退回本地猜测航向。

## 11. 2026-09-05启动、夹爪与相机时序简化

本次只修改三个局部行为，没有改变视觉协议、返航导航、对正或撞送距离：

- `TASK_START`删除原来的“到达后制动等待→Touch→单独转180°→等待5秒”子步骤。考虑出发障碍区，前600 mm由`Motor_MoveDistance()`以800 mm/s执行编码器定距＋IMU航向保持，舵机1保持55°且双爪保持Retract；完成后才把舵机1转到85°、同时打开双爪，并由`Motor_MoveSpin()`以最高900 mm/s在剩余约1.10 m内保持地面直线、闭环转约180°。总里程最后100 mm以160 mm/s收尾。
- `Claw_Open()`改为左108°/右72°同时发出；因此开局和安全区放置前都是同时开爪。`Claw_Touch()`保持左80°/右100°同时发出，但仍等待2秒才对上位机上报闭合。上电收纳`Claw_Retract()`继续先左23°、后右147°，避免改变最敏感的机械收纳顺序。
- 进入`TASK_WAIT_NAVIGATION`时不再把摄像头抬到90°，返航期间保持抓取观察角。只有收到`TASK_COMPLETE`确认物体已经送入、进入`TASK_EXIT_SAFE_ZONE`时，摄像头才转到120°，为下一轮搜索准备。

现场重点确认：旋转与平移叠加时轮速会由底盘混控器按统一上限分配，900 mm/s是中心巡航请求上限；先验证1.70 m终点和旋转方向，再根据实车惯性微调`APP_START_TURN_KP_MM_S_PER_DEG`和`APP_START_TURN_MAX_MM_S`，不要先改编码器距离。

## 12. 2026-09-05连续搬运与航向定距协议

- 对照上位机基线更新为`danmo-teng/shijue_fangan@c576447`，已应用其基于底盘`47d06f1`生成的`0001-Run-continuous-multi-cargo-rescue-cycles.patch`，保留补丁原作者信息。
- 新增命令`RETURN_CENTER=8`和`DISTANCE_VALID bit4`。`NAVIGATE_WAYPOINT`与`RETURN_CENTER`都要求bit1直行、bit2使用航向、bit4距离有效，载荷为`P2/P3=本段剩余距离、P4/P5=0、P6/P7=绝对航向`；非法组合直接拒绝执行。
- F407第一次启动定距时锁存编码器起点和命令距离，重复命令只刷新SEQ/看门狗，不会把已经行驶的路程重新执行。定距开始后命令超过1秒失联或收到STOP会故障停车，因为此时无法安全恢复完整原距离。
- `NAVIGATE_WAYPOINT`和`RETURN_CENTER`巡航上限均为800 mm/s；两者内部剩余距离小于300 mm后继续使用原有最高400 mm/s及线性减速。NAV命令在250～1000 ms宽限期内把巡航上限降到250 mm/s，并由电机层实际更新速度目标。
- 首次投送前，目标类别门控由RDK限制为普通物资；首件完成后RDK解锁普通、核心、危险和伤员。F407完成撞送后接受`TASK_COMPLETE`、抬相机到120°并后退0.45 m，随后在`CENTER`等待RDK根据当前T265位置发送一次返中航向和`距中心距离-600 mm`。本地定距完成后进入SEARCH，RDK看到该状态后清除旧类别和旧路线，开始下一轮。
- 目标重捕获执行500 ms静止观察、当前角度→140°→90°慢速舵机扫描、90°整圈扫描和1.00 m换位，最长60秒后回到SEARCH；连续模式不再使用180秒总时长停止，但通信、电机、IMU和机构看门狗仍保留。

## 13. 2026-09-05 NAV完成握手修复

- 现场出现底盘到达后LCD长期显示`STATE:NAV`、`CMD:NAV OK`。这表示F407定距已经完成，但RDK仍在重复NAV；不是夹爪动作失败。
- 根因是RDK `c576447`仅在T265地图判定车体圆接触安全区时才从NAV切换到ALIGN，没有消费F407编码器定距完成结果。定位有偏差时双方会稳定停在NAV等待。
- `TYPE=0x17 P0 bit5`现定义为`DISTANCE_DONE`。F407只在`TASK_NAVIGATE`且本段编码器定距完成时置位，并保持到RDK发来`ALIGN_SAFE_ZONE`。RDK应以“地图接触或新鲜NAV状态的DISTANCE_DONE”为切换条件；后续ALIGN、ENTER和张爪撞送安全门不变。
- 对队友`c576447`可直接应用`docs/rdk_patches/0001-consume-f407-distance-done.patch`；补丁同时包含状态解析、NAV切换和T265位置仍未接触时的回归测试。

## 14. 2026-09-05双高度搜索与静止车头重捕获

- SEARCH改为固定循环：相机120°旋转一圈→停车并快速命令相机到90°→稳定300 ms→90°再旋转一圈→恢复本轮起始航向→编码器闭环前进0.80 m→相机回120°开始下一轮。返中定距完成后的第一轮直接从90°开始；APPROACH丢失后的相机扫描仍保持每40 ms 1°慢速移动。两次转圈均用Location/IMU累计实际360°，不用定时估算。
- APPROACH只要失去合法单目标就立即停车，车头保持丢失时航向。静止500 ms后，相机从当前角度慢扫到140°，停留300 ms，再慢扫到90°并停留300 ms；每20 ms任务周期仍处理最新视觉报告，重新发现目标立即中断扫描并恢复APPROACH。
- 相机90°仍未找到目标时才允许底盘旋转一圈；再次失败后先回到丢失前车头航向，再以500 mm/s编码器闭环前进1.00 m并进入标准SEARCH。SEARCH的0.80 m和REACQ的1.00 m都使用IMU航向保持；预测落点越过200 mm场地余量时改朝场地中心，避免盲目出界。
- 每次进入SEARCH或REACQ都会记录当前视觉报告代次，只有之后到达的新报告可以触发目标恢复，因此慢速舵机扫描不会误用丢失前缓存的坐标。

## 15. 2026-09-05取消碰撞投送

- 根据现场视频，物块已由NAV末段直接送到放置区，原有“张爪→后退0.30 m→等待200 ms→以850 mm/s前冲0.55 m”的碰撞动作全部删除。为保持与RDK `c576447`的状态编号兼容，`TASK_RAM_BACK=13`和`TASK_RAM_FORWARD=14`枚举值保留为不可达占位，`TASK_RAM_VERIFY=15`继续作为上位机认可的投送确认状态。
- 收到`ENTER_SAFE_ZONE`且完成安全区航向对正后，F407只同时张开左右爪并进入`CHECK`原地停车。最短静止确认窗口由600 ms改为1200 ms；窗口内可以提前ACK上位机重复发送的`TASK_COMPLETE`，但必须满1200 ms后才进入`EXITSAFE`。
- 如果1200 ms后上位机仍发送`ENTER_SAFE_ZONE`，底盘继续原地等待，不再重复倒车或前冲。`ENTER_SAFE_ZONE/TASK_COMPLETE`仍必须保持250 ms内新鲜，失联或收到其他命令保持故障停车。
- 到达安全区后的慢速转动来自`ALIGN_SAFE_ZONE`，不是碰撞程序。底盘原地转向在剩余30°内从500 mm/s切换为120 mm/s；当前与RDK统一采用8°完成阈值、12°重新对正阈值。

## 16. 2026-09-05最小上位机改动与返中后90°搜索

- 保留现有`TYPE=0x18`航向＋距离协议、NAV/ALIGN/ENTER/TASK_COMPLETE/RETURN_CENTER命令和返中计算，不要求上位机增加新的路径字段。上位机P2/P3发送的是本段开始时计算并锁存的剩余距离，不是逐帧递减值；F407收到首帧后用本地编码器自行计算实时余量。
- F407普通SEARCH的120°首圈失败后不再逐度慢扫，直接命令舵机3到90°并等待300 ms再进行第二圈；逐度慢扫只保留在APPROACH目标丢失恢复。`RETURN_CENTER`定距完成后设置一次性低视角入口，下一次SEARCH直接从90°开始。
- RDK最小必需修改仅为删除碰撞语义：不再等待`RAM_FORWARD=14`，只在新鲜`CHECK/RAM_VERIFY=15`状态下累计安全区静止时间并发送`TASK_COMPLETE`。对应增量补丁为`docs/rdk_patches/0002-remove-collision-delivery.patch`，其余通信协议不变。

## 17. 2026-09-05可靠性收口

- F407和上位机统一以8°作为ALIGN完成阈值，误差达到12°才重新进入对正，形成4°迟滞；ENTER与ALIGN携带相同最终航向，延迟到达的一帧NAV只暂停当前对正，不会退回NAV或触发永久故障。
- 任务命令跳转收紧为`WAIT_NAVIGATION→NAV→ALIGN→ENTER→CHECK→COMPLETE`，不能再因合法但时序错误的新SEQ从后续状态跳回NAV/ALIGN。依赖Location的搜索、重捕获、NAV起步、ALIGN和返中转向统一采用1500 ms恢复窗：短暂失效停车，持续失效才报`POSE_TIMEOUT`。
- NAV末端比例由50%降为20%，并设置120 mm/s最低有效速度：NAV由800降到160 mm/s，RETURN由500降到120 mm/s，NAV通信宽限期也不低于120 mm/s，避免低到无法克服静摩擦。
- SEARCH快速切换相机角度后的300 ms稳定期不再锁定视觉目标；APPROACH丢失恢复的慢速逐度扫描仍持续接收目标。相机没有位置反馈，LCD的Angle是命令角度，300 ms只能作为机械稳定余量。
- 对RDK `c576447`新增一份一次性整合补丁`docs/rdk_patches/0003-rdk-navigation-delivery-rollup.patch`：包含DISTANCE_DONE消费、无碰撞CHECK、到达条件锁存、默认T265静止容差15→25 mm和对应回归测试。旧0001/0002仅保留便于逐项追溯，现场优先应用0003，不要重复叠加。

## 18. 2026-09-06低帧率视觉抗抖

- SEARCH收到1帧合法单目标就立即以350 mm/s进入APPROACH。进入APPROACH后，无目标新帧和没有新SEQ统一按“最后一次合法单目标”计时：250 ms内保持，250～600 ms把前进/转向线性降为0，600 ms后停车并冻结舵机，连续1200 ms没有合法目标才进入原500 ms重捕获流程；合法目标在窗口内恢复会立即继续追踪。

## 19. 2026-09-06适配RDK动态航向与剩余距离

- 对照上位机更新到`danmo-teng/shijue_fangan@bb8a138`：任务命令由定位串口线程100 Hz重发，NAV和RETURN载荷由任务进程按最新融合位置持续更新。此前F407“只锁存第一帧距离和航向”的实现已不兼容，也是LCD长期显示`STATE:NAV / D:1455`时底盘与上位机互相等待的重要原因。
- NAV与RETURN现在均不启动本地整段编码器定距，而是每20 ms读取最新`P2/P3=剩余距离`和`P6/P7=绝对航向`。剩余距离直接生成最高800 mm/s巡航及300 mm线性减速目标，本地IMU只负责执行绝对航向；NAV达到重对阈值时停车重新对向，RETURN则保持倒车并进行受限的连续航向修正。
- 动态载荷必须满足距离0～5000 mm、航向0～359.99°、直行/航向/距离有效位齐全。当前仅D=0 mm触发停车，停车后必须重新大于50 mm才恢复；任务命令超过250 ms立即停车；车辆前进时距离载荷连续500 ms没有至少2 mm变化会锁定等待，直到上位机距离重新变化，避免100 Hz心跳掩盖冻结的T265/融合位置。
- STOP和短暂命令中断现在可以安全恢复，因为恢复后采用上位机最新剩余距离，不会重跑旧整段。LCD的`D`已代表上位机最新实时余量；若车辆运动时D长期不变，故障在上位机融合位置或命令文件更新链路，不是F407内部里程。
- 旧的`0001/0002/0003`补丁均基于`c576447`，不得直接应用到`bb8a138`。其中碰撞清理尚未进入队友最新版，但当前F407只上报`CHECK=15`，上位机现有`{14,15}`判断仍能兼容运行；后续仅需在上位机删除不可达的14和旧“撞送”文字。

## 20. 2026-09-06底盘侧动态导航收口

- 动态H/D仍保持上位机控制结构，但速度目标增加1800 mm/s²加速、3000 mm/s²减速斜率，航向修正增加1800 mm/s²变化率；2°死区外按`(|error|-2°)×20`连续增加并限幅300 mm/s，不再从0跳到80 mm/s。
- NAV进入最后300 mm时将速度限制约400 mm/s；D≤100 mm后F407直接依据任务红蓝标志锁存红方90°/蓝方270°，先停车对正再推进，不再采用上位机后续heading。上位机发送D=0 mm时停车；由于20 ms任务周期和电机制动存在离散误差，0 mm是命令门限，不代表物理误差绝对为0。
- 非法距离/标志/命令现在报告`COMMAND_TIMEOUT`，只有转向执行失败才报告`MOTOR`。TaskStatus新增`nav_stale/nav_done`供LCD显示`STALE/DONE`，避免把100 Hz心跳正常误解为规划数据有效。
- SEARCH首次锁定合法单目标时保存其类别位，APPROACH、REACQ、GRAB_OBSERVE/RAISE/ROTATE全程必须匹配该类别；进入新SEARCH才解除锁定。协议仍无法携带置信度和bbox，首帧误识别必须由RDK阈值负责。
- SEARCH 90°、REACQ 90°和抓取观察旋转现在每周期检查Location；失效立即停车，连续1500 ms无效报告`POSE_TIMEOUT`，不再因已有起始航向缓存而继续盲转。

## 23. 2026-09-08导航角度可视化

- 正常Task的LCD导航行现在显示`T:xxx A:xxx D:xxxx`：`T`是上位机最新任务目标航向，`A`是F407本地Location/IMU当前航向，`D`是上位机最新剩余距离。Location无效时`A:---`，不伪造角度。
- 该显示用于区分“上位机目标方向错误”和“F407执行方向偏差”；不会改变导航控制。上位机仍需在放置区前约250～300 mm把目标heading切换为放置区方向，F407只执行该新heading并在最后300 mm以约400 mm/s行驶。

## 24. 2026-09-08最后100 mm航向锁存

- NAV首次进入`D≤100 mm`时无条件切换为本地固定放置区法向：命令flags为红方时90°，蓝方时270°。F407立即停止平移，不论当前车头方向如何都先用IMU原地对正到误差≤2°，再以约400 mm/s推进最后100 mm；这一区间忽略上位机后续heading变化。
- 上位机距离因定位回跳重新大于150 mm时解除锁定，恢复动态航向控制；ENTER、RETURN或新一轮NAV同样自动清除锁定。该迟滞避免100 mm边界附近来回切换。
- LCD未锁定时显示`T:目标 A:实际 D:余量`，锁定后显示`L:090/270 A:实际 D:余量`。RETURN_CENTER不使用近距离锁存，仍持续跟随上位机返中航向。
- 动态导航停止门限由2 mm改为严格`D=0 mm`。由于协议距离非负，代码中的`<=0`等价于仅0触发；一旦停车，余量在0～50 mm内继续保持停车，超过50 mm才允许恢复，避免零点噪声反复启停。

## 21. 2026-09-07删除安全区车头对正并返回中心点

- `TASK_ALIGN_SAFE_ZONE=11`和`VISION_CMD_ALIGN_SAFE_ZONE=4`保留数值兼容，但正常状态机不再进入或执行对正。F407只允许`TASK_NAVIGATE`直接接收`ENTER_SAFE_ZONE`，随后张爪进入`CHECK`；旧上位机继续发送ALIGN时底盘只停车等待，不会旋转，也不会误报故障。
- 投送完成后的返中仍先执行0.45 m编码器闭环后退，以免车体贴围栏原地转向；该动作完成后立即进入RETURN并跟随上位机动态航向/余量，不再增加任何放置区车头对正步骤。
- 上位机必须基于`437c0ef`同步把到达分支从`NAV→ALIGN`改为`NAV→ENTER_SAFE_ZONE`，删除ALIGN确认状态；`center_stop_radius_m`和CLI默认值从0.60 m改为0，校验允许0，使RETURN剩余距离直接等于`hypot(x,y)`并到达场地原点。否则旧上位机会停在ALIGN，或仍只返回距中心600 mm的位置。

## 22. 2026-09-07 IMU校准先于舵机动作

- 原启动顺序是`main.c: Servo_Init() → Robot_Init() → IMU_Init()`；`Servo_Init()`会立即启动TIM8四路PWM并把全部舵机命令到90°，机械振动会发生在陀螺仪静止零偏校准之前。
- `Servo_Init()`现移入`Robot_Init()`并严格放在`IMU_Init()`成功之后。IMU执行芯片复位、配置、稳定等待和128点静止校准期间TIM8 PWM不启动；校准完成后才启动PWM并由Task执行舵机1、摄像头和左右爪初始化。
- 新增`application_ready`门控。IMU初始化失败时不调用`Servo_Init()`、不首次运行Task；后续PendSV任务也要求`application_ready && IMU_GetData().ready`，避免失败或运行时IMU故障后继续触发机构/运动状态机。
- 视觉PID只在新报告代次到达时更新，X/Y采用新样本权重0.35的一阶低通；最大PID `dt`由100 ms放宽到300 ms，避免低帧率时微分项被人为放大。舵机3视觉跟踪按30°/s限制变化率，低帧率不会再表现为每帧突然跳6°。
- SEARCH的120°和90°每个视角一整圈只要求至少1帧新的合法报告；整圈完全没有报告才重置IMU转圈计数并保持当前视角继续搜索。快速切90°后的300 ms稳定期仍不锁定目标，慢速REACQ扫描继续逐帧接收目标。

## 26. 2026-09-08最新版RDK投送确认适配

- 最后100 mm仍由F407按赛前锁存颜色固定为红方90°/蓝方270°，但停车原地旋转的完成门限由2°放宽到4°；行进中的连续航向修正死区仍为2°，因此只减少末段原地等待，不放宽直线推进精度。
- 配置帧锁存本方颜色后，NAV、RETURN和ENTER的`RED_SIDE`必须与锁存颜色一致；NAV/RETURN继续要求直行、最终航向和距离有效位，ENTER要求直行和最终航向有效位。方向标志不一致时不会按错误方向推进。
- 收到ENTER后先在140°抓取视角完成双爪同步张开，再把摄像头转到120°并稳定300 ms，之后才上报`RAM_VERIFY/CHECK=15`。上位机可在稳定画面中连续确认物资由安全区外进入区内；CHECK最短1200 ms窗口及TASK_COMPLETE握手不变。
- F407不对RETURN距离私加600 mm。队友最新版若仍使用`center_stop_radius_m=0.60`，D=0表示距原点约600 mm；若要求真正回到原点，必须由上位机改成0并允许该配置值。

## 27. 2026-09-08 D=0末端补推

- NAV首次收到`D=0 mm`后不立即完成，而是保持已锁存的红方90°/蓝方270°，以180 mm/s继续慢推，至少持续1.0 s、最长1.8 s；只有满1.0 s后编码器累计360 mm才允许提前停车。RETURN_CENTER不执行该补推。
- 补推期间不运行“余量500 ms无变化”判断，也不提前置`DISTANCE_DONE`；LCD命令状态显示`PUSH`。补推完成后才置位并保持停车，避免上位机定位提前归零导致物资没有完全进入。
- 最新RDK可能根据围栏几何或视觉确认在D=0前发送ENTER。F407会ACK但不立即张爪，先以同样的固定法向完成一次补推，再进入OPEN和CHECK；命令超过250 ms、位姿无效或电机故障时仍优先停车。

## 28. 2026-09-08末段精度与返中搜索顺序

- 放置区末段原地对正门限由4°收紧到1°，连续航向修正死区由2°收紧到1°，动态航向重新停车对正门限由7°收紧到3°。相对转向完成并稳定后必须重新读取Location绝对航向，误差仍≤1°才允许推进，避免制动惯性造成偶发倾斜。
- 正常开局SEARCH顺序仍为摄像头120°一圈→90°一圈。投送完成并RETURN_CENTER到达后改为90°一圈→若无目标则摄像头120°稳定300 ms后再转一圈；任意一帧合法目标都会立即进入APPROACH，两圈都确认无目标后才恢复本轮起始航向并前进0.80 m。

## 29. 2026-09-08删除返中前0.45 m后退

- CHECK收到`TASK_COMPLETE`后从mode15直接进入`FACE_FIELD_CENTER(mode17)`，原0.45 m编码器后退和对应速度/距离参数已删除；保留`TASK_EXIT_SAFE_ZONE=16`编号兼容，但正常流程不可达，即使异常进入也会立即转mode17而不驱动车辆。
- 最新RDK在COMPLETE看到新鲜mode17后进入RETURN_CENTER，并在T265融合位姿有效时自动持续发送命令8的绝对航向和剩余距离；F407原地等待首帧合法RETURN后再运动，协议超时仍停车。
- RDK当前默认`center_stop_radius_m=0.60`，因此其D=0仍代表距离场地原点约600 mm。若目标是严格返回原点，需要上位机把该配置改为0并允许0值，F407不应私加距离补偿。

## 31. 2026-09-08直接倒车返中与普通Debug建图

- 放置确认收到TASK_COMPLETE后仍直接进入mode17等待上位机RETURN，但RETURN不再按上位机行进航向先原地掉头。F407把车体目标航向设为`上位机H+180°`，从静止直接给负前进速度倒车；沿途仅以最大100 mm/s旋转分量跟随H的小变化，不触发3°原地重新对正。上位机D和失联停车规则不变。
- 普通`Debug`构建默认改为`APP_MODE_MOTION_DEBUG_TASK`，主循环调用完整`Main/Src/Debug.c`而不调用Task.c；不用选择DAPLink MotionDebug。NormalRun仍显式覆盖为正式救援Task，后续恢复比赛程序时重新烧录NormalRun即可。
- `Debug.c`最初按b03a641的旧0x17/0x18调试程序实现；队友最新版4007ae0的桌面`t265_map`已改用独立0x19扫描命令和0x1A扫描状态，本地现已同步，并实现STOP/HOLD/TURN_REL/MOVE_BODY/MOVE_FIELD/RESET_ODOM。LCD以`RX:True/False`显示是否收到过合法0x19命令；正常Task的0x17/0x18完全不变。

## 30. 2026-09-08 APPROACH大幅摇摆抑制

- 只调整视觉靠近控制，不改变SEARCH、抓取、返安全区、末端慢推、放置和RETURN状态机。水平PID由Kp 1.12/Kd 0.0298改为0.72/0.020，最小旋转由80降到40 mm/s，限幅由239降到175 mm/s。
- 新增1000 mm/s²旋转目标变化率，视觉误差换边时必须逐步经过0，不能一帧从正限幅跳到负限幅。旧转向只保持120 ms，随后在300 ms前按帧龄衰减至0；前进的250/600/1200 ms保持、停车和重捕获门限不变。
- 坐标滤波新样本权重由0.35提高到0.55，减少低帧率闭环相位滞后；抖动改由旋转斜率吸收。水平误差≤48 px使用原定速度，48～160 px线性限速，≥160 px时最多150 mm/s；已有500/250 mm距离限速仍优先，绝不会因对中限速把近距离速度反向提高。
- RDK当前按允许类别中检测框面积最大者逐帧选目标，若同类物体同时可见仍可能发生目标切换；F407的平滑限幅会降低突跳影响，但根治需要上位机在APPROACH锁存同一track ID。

## 32. 2026-09-09目标确认与长物体预停车

- SEARCH进入APPROACH的确认门限最终恢复为1帧；单帧低延迟优先，误识别和多目标选择必须由上位机的置信度、目标尺寸、距离及目标跟踪负责。
- APPROACH的正常抓取条件仍为`NEAR=1`、有效距离≤120 mm或舵机3命令达到140°。135～139°区间只把前进限到80 mm/s；若同一锁定类别持续有效、该角度区间连续保持700 ms且横向误差≤96 px，才锁存停车并非阻塞地把舵机3命令移到140°进入抓取观察。闭爪仍只接受上位机`GRAB_CONFIRMED`，避免普通物块刚到135°就被提前截停。
- NAV的D=0末端慢推最终调整为180 mm/s：强制最少1.0 s，目标总时长1.8 s；理论总推进约324 mm，编码器安全上限取360 mm且只在满1.0 s后生效，避免里程累计偏大导致约0.5 s提前停止。

## 33. 2026-09-09首次投送双侧左偏补偿

- 只在上电后的第一次投送中，对D≤100 mm锁存的最终放置航向分方向补偿：红方右偏10°（90°→80°），蓝方左偏10°（270°→280°）。途中上位机动态NAV航向、RETURN返中和搜索均不受影响。
- `TASK_COMPLETE`通过新鲜度和状态前驱检查并被`TASK_RAM_VERIFY`接受后，才把首次投送标志置为完成；第二次及以后自动恢复红方90°、蓝方270°。重复任务命令不会重复累计偏置。
- D=0的180 mm/s末端补推复用同一个已锁存航向，因此第一次补推也保持80°/280°；LCD的`L`字段会显示实际锁存值，便于现场确认补偿方向。

## 34. 2026-09-09统一90°搜索与本地返中心

- 普通SEARCH和APPROACH丢失恢复统一为同一入口：丢失后先停车500 ms；随后舵机3从当前命令角直接以1°/20 ms移动到90°，不再经过140°和中间停留，稳定300 ms后以200 mm/s原地转满360°。相机移动、稳定、旋转、对正和返中心期间都继续接收新报告，任意1帧合法单目标可立即进入APPROACH。
- 一圈收到过视觉报告但未找到目标时，删除原SEARCH前进0.80 m和REACQ前进1.00 m。F407读取本地Location坐标，距离原点超过100 mm时计算`atan2(-y,-x)`，先用IMU原地对正，再以850 mm/s调用`Motor_MoveDistance()`，通过编码器定距和IMU保持航向返回`(0,0)`；到达后重新进入90°搜索。
- 已在中心100 mm范围内时不做短距离动作，直接重置IMU累计转圈检测并继续搜索。若整圈没有收到任何新视觉报告，则视为视觉链路不足，原地重复本圈而不盲目返中。

## 35. 2026-09-09完整比赛流程协议与快速受控动作

- 对齐`danmo-teng/shijue_fangan`的`codex/complete-rescue-flow@bdcf0f2`，扩展同一`TYPE=0x18`中的命令9～18。完整流程激活后，`APPROACH_TARGET`的原生X/Y是PID唯一输入，避免再与旧0x12目标争夺控制权；HOLD在外部运动阶段立即停车，在START/SEARCH等底盘自主阶段不阻断既定出发和扫描。
- 新增夹内审核锁存。稳定审核必须满足计数一致、总数1～3、无危险/未知且伤员不混装；首件正式投送还必须恰好1件绿色。`bdcf0f2`在第3个一致视觉帧直接改发GRAB，没有把STABLE审核帧写入串口，因此F407兼容为“连续2个相同审核命令”或“1个显式STABLE命令”建立稳定门。非法审核拒绝GRAB，并按上位机指定执行单侧分离：释放侧完全打开，保留侧只到正常触碰角（左80°/右100°），不会压到23°/147°收纳极限。退让后舵机3低头到140°、稳定300 ms并等待重新审核；复审合法后收到`GRAB_CONFIRMED`会重新把双爪合到左80°/右100°，经过2秒机构确认才等待NAV，防止运输途中物资滑出；复审再次失败才双爪全开。临时藏堆允许1～3件已知且不含伤员混装的物资，NAV到点后双爪释放。
- 新增单爪机构接口，但正常抓取和投送继续双爪同步。完全张开角仍为左舵机4=108°、右舵机2=72°。
- 新动作使用非阻塞子状态机：退让750 mm/s；脱困旋转500 mm/s、横移800 mm/s；换道850 mm/s；打散先张爪，再以800 mm/s执行0.18 m前进/0.24 m后退和两侧短拨，穿插+25°/-50°/+25°快速旋转后恢复原方向。命令新SEQ只更新ACK，不会重启动作；失联停车，动作总时限7秒。
- `TYPE=0x17`使用独立映射输出新版mode 20～22和30～36，不重排已有TaskState。LCD补充APP坐标、AUD左右类别/总数和全部新动作名称。
- 上位机当前返中在距原点0.60 m时直接进入SEARCH并发送HOLD，未完成严格的RETURN D=0握手。底盘临时只在最近RETURN余量≤650 mm时把该HOLD视为返中完成；上位机后续仍应改为持续发送RETURN D=0并等待F407上报SEARCH。
- 上位机当前`_audit_valid()`对`initial_stash`只检查总数1～3，会把危险物或伤员混装也当成可抓。F407会安全拒绝该GRAB，但上位机会停留在GRAB等待。上位机必须把危险、未知和伤员混装判断移到`initial_stash`提前返回之前，并进入`INVALID_RELEASE`，否则这类场景无法自动恢复。
- 上位机的非法物资流程必须由`INVALID_RELEASE -> INVALID_BACKOFF -> SEARCH`改为`INVALID_RELEASE -> INVALID_BACKOFF -> CAPTURE_AUDIT`。`YIELD_DONE`后重新发送审核帧；审核合法则持续发送`GRAB_CONFIRMED`，直到F407完成双爪重新合拢并上报`GRIPPER_CLOSED=1`，审核仍非法则发送`RELEASE_BOTH`。旧流程在单侧分离后直接SEARCH会被F407的复审门拒绝，表现为安全停车而不是夹着旧物资继续抓新目标。

## 36. 2026-09-09删除盲打乱与单侧分离退让

- 删除的是F407到中心后无条件执行的盲打乱。上位机确认首件绿色物资无法单独取得时主动发送`DISPERSE_PILE`的视觉受控打散必须保留；F407只在空爪且没有等待夹内复审时接受。
- 单侧释放后的`YIELD_BACKOFF`改为两段编码器定距且继续使用IMU航向保持：最初100 mm为450 mm/s，剩余距离恢复750 mm/s。退让完成后舵机3到140°并稳定300 ms，之后才上报`YIELD_DONE`。
- 对照上位机`codex/complete-rescue-flow@8339c1b`，其首件绿色物资无法单独取得时发送`DISPERSE_PILE`的逻辑正确，应继续保留；但`INVALID_BACKOFF`完成后清空批次直接进入SEARCH仍需改为夹内复审。

## 37. 2026-09-09加强视觉受控打散

- `DISPERSE_PILE`从短距离直推改成三方向强冲击：双爪完全打开后先以900 mm/s后退0.30 m蓄力，再以1000 mm/s正面前冲0.50 m；随后两次各后退0.38 m，并执行0.48 m弧线前冲，第一段同时转+60°，第二段同时反转120°，最后后退0.38 m并原地+60°恢复初始航向。
- 新增弧线执行器，同时使用Location路径累计和IMU累计转角闭环；距离先完成则只继续旋转，角度先完成则只继续直行，两者均完成才进入下一段，避免纯计时动作造成累计漂移。
- 正面冲击速度1000 mm/s达到当前电机软件上限；弧线前冲和退让为900 mm/s，弧线旋转分量350 mm/s。打散专用总超时提高到15秒，其他远程动作仍为7秒。上位机必须等待新鲜`mode=35/DISPERSE_DONE`，固定5秒提前退出会用HOLD中断尚未完成的强打散，保护超时应提高到至少16秒。

## 38. 2026-09-10对齐上位机`codex/gamepad-teleop@cc492d9`

- 上位机已增加`WAIT_SEARCH_RECOVERY`：APPROACH目标连续丢失1.2秒后，在定位、视觉、STM状态均新鲜、夹爪未闭合且没有夹内复审时停止刷新HOLD，等待F407按现有逻辑经过`mode20 -> mode24 -> mode3`。目标重新出现会清除连续丢失计时；3秒仍未恢复则发送ABORT。F407不再另加一套目标丢失切换逻辑。
- F407保留打散HOLD防死锁：未完成打散收到HOLD立即停车；1秒内重新收到同一`DISPERSE_PILE`可继续，HOLD持续1秒则取消本轮并回到SEARCH。该保护只是兜底，上位机开始打散后仍应锁存许可并持续同一命令直到新鲜`mode35`，不能因运动模糊、目标短暂离开画面或一帧视觉超时而改发HOLD。
- 对齐上位机现有`RETURN_STASH`：空爪、SEARCH状态下收到字段完整且范围合法的`NAVIGATE_WAYPOINT`，F407把它识别为非投送藏点路线。该路线禁用安全区航向锁定和最后补推；到D=0置`DISTANCE_DONE`。若上位机按地图紧容差略早切换SEARCH/HOLD，只有最近一次真实剩余距离不大于100 mm才接受交接并进入SEARCH，远距离HOLD仍只停车。
- 正常夹物资NAV、D≤100 mm锁定本方安全区方向、首件±10°补偿、D=0后的编码器限时补推、投送视觉确认以及倒车返中流程均未改变。
- 当前上位机仍需优化三点：打散动作开始后锁存执行许可；区分“正常SEARCH心跳”“视觉故障停车”“任务结束停车”，因为F407在SEARCH中会继续执行自主扫描而不会把HOLD解释为永久停车；`DELIVERY_VERIFY`增加超时后的重新观察或明确故障，避免视觉无法形成“区外后连续5帧区内”时双方永久停在mode15。
- 上位机默认仍启用开局临时搬堆，命令行`--disable-initial-stash`才关闭；比赛最终策略必须明确。当前空爪回藏点已能执行，但建议上位机优先等待新鲜`DISTANCE_DONE`或D=0再切回SEARCH，不再仅凭地图位置容差提前结束。

## 25. 2026-09-10删除重复超时依赖与两层SEARCH

- 完整流程只使用`TYPE=0x18`时，SEARCH无目标期间上位机发送HOLD，不会产生旧`TYPE=0x16`的`report_generation`。因此删除“每圈至少收到一帧旧视觉报告才允许结束”的门槛，避免无目标时永远重复同一圈。
- SEARCH固定为舵机3在90°旋转一圈，再切到120°旋转一圈；任一阶段收到进入本轮以后产生的合法`APPROACH_TARGET`都立即进入APPROACH。两圈均无目标时，离中心超过100 mm则用Location/IMU和编码器闭环返回`(0,0)`，已经在中心则从90°重新搜索。
- START读取Location改用统一的1500 ms恢复窗：单次无效只停车等待，持续无效才进入`POSE_TIMEOUT`。
- NAV、RETURN、OPEN_FOR_RAM和RAM_VERIFY中的任务命令短暂过期或阶段暂时不匹配时只停车并保留状态，不再产生锁存的`COMMAND_TIMEOUT`。明确STOP/ABORT仍立即进入STOPPED，电机、持续位姿和内部状态故障仍保留原保护。
- 远程动作遇到任务命令断流或不匹配时记录暂停时间；收到同一动作的新鲜命令后从原阶段继续，暂停时间不计入7秒/15秒物理动作超时，避免通信调度延迟被误报成电机故障。
- 上位机不应再复制F407的250/600/1200 ms APPROACH帧龄控制，也不应在普通目标丢失、ID变化或恢复等待时发送ABORT。高层只保留长期STM失联、明确STM故障、确认越界和用户急停；其余异常统一降级为HOLD/SEARCH，并让F407完成短周期底盘保护。

## 39. 2026-09-10修复TYPE=0x18逐帧跟踪与ACTION越状态执行

- 运行记录曾出现上位机已经进入SEARCH并持续发送HOLD、F407仍停在`mode=20`的状态分裂。现在APPROACH收到HOLD仍立即停车，但不会冻结恢复计时：距最后合法目标达到1200 ms进入`mode=24`，再静止500 ms进入`mode=3/SEARCH`。最新版上位机的`WAIT_SEARCH_RECOVERY`仍应停止刷新HOLD并等待该握手；底盘处理持续HOLD只是防止双方再次互相等待。
- `TYPE=0x18/APPROACH_TARGET`转换原先只递增`report_generation`，而水平PID和舵机3仍用旧`TYPE=0x12`的8位`sequence`判重，导致完整流程通常只处理第一帧坐标。现统一使用32位`report_generation`作为目标帧标识，两种目标协议都能逐帧刷新X/Y滤波、水平转向和相机俯仰。
- SEARCH自身没有横移。为防止relay残留的新SEQ动作帧抢占SEARCH，`YIELD_BACKOFF`只允许APPROACH、NAV、RETURN或已完成的释放/脱困动作进入，`ESCAPE_MANEUVER`只允许上述运动状态或已完成YIELD进入，`CHANGE_LANE`只允许APPROACH/NAV进入；SEARCH仅保留合法`DISPERSE_PILE`进入ACTION。重复的同一动作仍可在ACTION中ACK，不会重启动作。
- 回归重点：连续发送不同SEQ且Y坐标变化的APPROACH帧时LCD Angle必须连续变化；X误差收敛后旋转量必须同步下降；`mode=3`下注入YIELD/ESCAPE/LANE不得进入ACTION，合法DISPERSE仍应进入并最终上报`mode=35`；持续HOLD应观测到`mode20 -> mode24 -> mode3`而不是永久停在mode20。
